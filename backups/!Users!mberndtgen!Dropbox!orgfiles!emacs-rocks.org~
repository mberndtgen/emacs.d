* Emacs Rocks! exercises
** DONE Do something
CLOSED: [2018-02-08 Do 12:40]
** TODO One more
** TODO Another one
** TODO ...and another one (M-S-RET) 
Press C-c C-t on a TODO and then it's done, timestamp included. That's
different when pressing C-t C-c on a 'Done': then it simply disappears.

A [[http://test.invalid/][link]].

#+BEGIN_SRC sh
  ls
#+END_SRC

Mark a point,
Select the end of the mark (be it through a search or whatever)
C-x r t <string>
replaces with <string> whatever is in the rectangle 


C-x C-t: same with two Line
M-t: transpose word with word before it
C-t : Transpose (swap one letter with the one before it)

whatever gets transposed moves foreward

C-<number> : do next command <number> times

#+BEGIN_SRC emacs-lisp
 (first '(z a bq c))

#+END_SRC

#+RESULTS: 


#+BEGIN_SRC clojure :results output :show-process t
  (dotimes [n 10]
    (println n ".")
    (Thread/sleep 500))
#+END_SRC

#+RESULTS:

#+BEGIN_SRC emacs-lisp
  (defun org-xor (a b)
     "Exclusive or."
     (if a (not b) b))
#+END_SRC

F3: begin macro
F4: end macro

M-l: lowercase (whole word, moves to end of word)

M-u: uppercase (whole word, moves to end of word)
M-f: one word foreward
M-b: one word backword

move along parentheses:

- C-M-n: next 
= C-M-p: previous


word1, blab 
balb laba word2 


"  " var abla1
"  " var bbla2
"  " bar Ycbla3


acbdefg
hijklmno

